<!DOCTYPE html>
<html lang="ja">
  <head>
 　<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>しぶ＆れいな 2次会でtweet:list</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap.min.css" rel="stylesheet" media="screen">
    <link href="/css/bootstrap-resposive.min.css" rel="stylesheet" media="screen">
  </head>
<html>
  <body>
    <script src="http://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <% if (comments) { %>
    <div class="row-fluid">
    <div class="span">
      <table class="table table-striped table-condensed">
        <tbody>
          <% comments.forEach(function(list){ %>
          <tr>
            <td>
            <div><span><%= list.user %></span><span style="float: right;"><%= list.CREATE_HMS.substr(0,2)+":"+list.CREATE_HMS.substr(2,2)+":"+list.CREATE_HMS.substr(4,2) %></span></div>
            <div style="clear: right;"><%= list.comment %></div>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    </div>
    <% } %>
  </body>
</html>
